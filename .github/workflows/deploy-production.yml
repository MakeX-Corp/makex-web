name: Deploy to Trigger.dev Production

on:
  push:
    branches:
      - main
  workflow_dispatch: # Allow manual triggering

jobs:
  deploy-production:
    runs-on: ubuntu-latest
    environment: production
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build project
        run: yarn build

      - name: Deploy to Trigger.dev Production
        env:
          TRIGGER_SECRET_KEY: ${{ secrets.TRIGGER_PRODUCTION_SECRET_KEY }}
        run: |
          npx trigger.dev@latest deploy --env production --config ./trigger.config.ts

      - name: Notify deployment success
        if: success()
        run: |
          echo "🚀 Successfully deployed to Trigger.dev production environment"
          echo "Environment: production"
          echo "Branch: ${{ github.ref_name }}"
          echo "Commit: ${{ github.sha }}"

      - name: Notify deployment failure
        if: failure()
        run: |
          echo "❌ Failed to deploy to Trigger.dev production environment"
          echo "Please check the logs above for more details" 